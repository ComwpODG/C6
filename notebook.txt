Kaelin's notes:

- Stars should be upright
    - find world coordinate on the screen and don't apply the slant modifier
    - create function that computes screen position from slanted plane and add it to stars

- When zooming out, stars should fade out instead of shrinking ✔
    - also applies to jumplanes
        - move jumplanes to a separate plane

- Clicking a star should open an info panel that shows
    - system name ✔
    - galactic sector - this should be precomputed
    - owner (default message if no owner) ✔
    - thumbnail image
    - list of information blocks below it
    - examples of occupying forces or notable units
    - coloured according to faction
    - if data is missing, don't even show the heading for the section
    - IF sector has description, it appears under faction ownership even if no owner
    - refer to pinned message for formatting ✔

- Each star should support a list of markers/icons
    - center aligned above the star, works the same as center alignment in text editors

- Need territorial lines / areas
    - defined as closed sets of vertices
    - manually defined
    - color the space with the faction colour
- dropdown player select
    - uses the player name to load the file "playername.json"
    - this file holds all data that isn't star name, sprite, position and jumplane
        - sprite can be overridden


Suggested phase 1
- When zooming out, stars should fade out instead of shrinking ✔
- Stars should not lie flat on the slanted table ✔
- Stars should appear upright ✔
- Make stars clickable ✔
- Add info panel with basic system data that appears on click ✔
- Add per-system default/custom status text
- Add zoom threshold for labels ✔
- Add per-star stacked markers/icons
- Add manual colored territory polygons

trim, force lowercase, hash algorithm -> hashmap key
at hashmap data, hold Name, all other relevant data
on player data load, for each hashmap entry, use Name to check if player file has entry, if yes load overrides and rest of data, else continue
on display, show hashmap key for FED name and actual name for ENI name
check icons field, for each entry load icon, draw them based on count
check optional fields. If exists AND not null, display data, otherwise dont display

hash algorithm should be: if A is 1 and B is 2, AB becomes (int)12, dont care about two digit numbers
-> might lead to collisions!
    - edge case of two letters represented by 2 digits (L -> 12) can also be represented by two lower letters (AB -> 12)
    - was given creative liberty in finding a solution


Com suggests:
You neeeeeeed to key a hashmap of stars by their name (you can condense it to a number after forcing it to lowercase internally to make computation easier)
so that when you read the player file you can identify and update these stars quickly
when i say you need to "key" it, 
i'm telling you to use a hashmap where the star is stored as the value for the ID that is it's name converted into a number. 
When you load the stars, you load them into that hashmap. Then you load the player file. 
Player file should be human-readable
    - i want to be able to type the name of the star system (wether it is ENI or FED name for it, IDFK lol, that one i leave to you.)
    - player file has the same algorithm to determine ID, so you will be able to do a lookup on the hashmap and get the star data / modify it for display.



Wishful thinking / later exploration
- moving ships around through the UI
    - should also have status box on click
- Add completely client-side drawing on the map
- Automatic territory generation from owned systems, if possible


tickmark - ✔

use python -m http.server 8000 to open a server on http:\\localhost:8000 and that's where everything will be working fine